
apiVersion: v1
kind: Service
metadata:
  name: welcome-service
spec:
  type: NodePort
  selector:
    app: welcome-app
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30080
